# When using separate Vite dev server (development only):
# VITE_COLYSEUS_URL=ws://localhost:3000
# VITE_API_URL=http://localhost:3000/api

# When running from backend server (production or built):
# Leave these blank - will use same-origin (automatic)
VITE_COLYSEUS_URL=
VITE_API_URL=
