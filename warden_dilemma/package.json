{
  "name": "warden-dilemma",
  "version": "0.1.0",
  "private": true,
  "description": "A platform for studying strategic behavior in N-player iterated prisoner's dilemma games",
  "scripts": {
    "dev": "pnpm build:client && pnpm --filter warden-dilemma-server dev",
    "dev:watch": "concurrently \"pnpm --filter warden-dilemma-client dev:watch\" \"pnpm --filter warden-dilemma-server dev\"",
    "dev:separate": "concurrently \"pnpm --filter warden-dilemma-server dev\" \"pnpm --filter warden-dilemma-client dev\"",
    "build": "pnpm build:client && pnpm build:server",
    "build:client": "pnpm --filter warden-dilemma-client build",
    "build:server": "pnpm --filter warden-dilemma-server build",
    "start": "pnpm --filter warden-dilemma-server start",
    "test": "pnpm --filter warden-dilemma-server test && pnpm --filter warden-dilemma-client test",
    "clean": "pnpm --filter warden-dilemma-server clean && pnpm --filter warden-dilemma-client clean"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
