#!/usr/bin/env bash
set -euo pipefail

HERE="$(cd "$(dirname "$0")" && pwd)"
ROOT="$(cd "$HERE/.." && pwd)"
cd "$ROOT"

echo "[phase2] Placeholder. Implement training loop for envs/phase2_link.py"
echo "See configs/phase2.yaml for intended knobs."

