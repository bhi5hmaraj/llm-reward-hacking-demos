seed: 123
max_steps: 200

model:
  base_model: gpt-3.5-turbo-1106

training:
  learning_rate: 5.0e-5
  kl_coeff: 0.02
  group_size: 4
  epochs: 40
  rollouts_per_update: 64

env:
  map_name: alttp_demo
  betrayal_bonus: 50
  visible_reward: 1
  actions: [U, D, L, R, USE]

